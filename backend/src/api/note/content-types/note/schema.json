{
  "kind": "collectionType",
  "collectionName": "notes",
  "info": {
    "singularName": "note",
    "pluralName": "notes",
    "displayName": "Note"
  },
  "options": { "draftAndPublish": true },
  "attributes": {
    "title": { "type": "string", "required": true },
    "content": { "type": "richtext" },
    "type": { "type": "enumeration", "enum": ["note", "research", "competitor", "meeting", "other"], "default": "note" },
    "idea": { "type": "relation", "relation": "manyToOne", "target": "api::idea.idea" }
  }
}
